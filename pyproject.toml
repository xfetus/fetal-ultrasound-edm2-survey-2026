[project]
name = "rater-auc"
version = "0.1.0"
description = "Compute and plot analysis for real vs synthetic discrimination"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.3",
    "matplotlib>=3.11.2",
    "scikit-learn>=1.9.1",
]
